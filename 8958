t = int(input())

for _ in range (t):
    results = list(input())

    score = 0
    count = 0

    for x in results:
      if x == "O":
        count += 1
        score += count
      else:
        count = 0
    print(score)
