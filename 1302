N = int(input())
count = {}

for _ in range(N):
    book = input()
    count[book] = count.get(book, 0) + 1

max_count = max(count.values())

result = []

for book in count:
    if count[book] == max_count:
        result.append(book)

result.sort()

print(result[0])
